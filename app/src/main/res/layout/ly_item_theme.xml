<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?android:selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <RelativeLayout
        android:id="@+id/ll_theme_item"
        android:layout_width="match_parent"
        android:layout_height="@dimen/theme_item_height"
        app:layout_constraintTop_toTopOf="parent">

        <View
            android:id="@+id/v_color_block"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:layout_marginStart="16dp"
            android:background="@drawable/bg_rounded_rectangle" />

        <TextView
            android:id="@+id/tv_theme_item_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_marginStart="20dp"
            android:layout_toEndOf="@id/v_color_block"
            android:text="color_theme" />

        <ImageView
            android:id="@+id/iv_theme_list_check"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:layout_marginEnd="30dp"
            android:background="@color/transparent"
            android:contentDescription="@null" />

    </RelativeLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="@color/line_grey"
        app:layout_constraintTop_toBottomOf="@+id/ll_theme_item" />

</androidx.constraintlayout.widget.ConstraintLayout>